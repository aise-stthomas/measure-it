[project]
name = "measure-it"
version = "0.1.0"
description = "AI Systems Engineering, Project 1: an evaluation harness for one model call."
requires-python = ">=3.12"
dependencies = [
    "google-genai>=2.23",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
