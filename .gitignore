# Secrets — the Gemini key lives in .env and never in a commit
.env

# Fixtures ARE committed: they are your data, and the graders re-score from them.

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.DS_Store

# A large golden set that stays out of the repository (instructor's full set; drafts)
golden/golden-full.jsonl
golden/*.local.jsonl
